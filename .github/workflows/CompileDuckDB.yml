name: Compile DuckDB
on:
  workflow_call:
    inputs:
      duckdbRepo:
        description: "duckdb repository"
        required: false
        type: string
        default: duckdb/duckdb
      ref:
        description: "duckdb branch, tag, or full-length SHA"
        required: true
        type: string
    outputs:
      duckdb-sha:
        value: ${{ jobs.compile-duckdb.outputs.duckdb-sha }}

  workflow_dispatch:
    inputs:
      duckdbRepo:
        description: "duckdb repository"
        required: true
        type: string
        default: duckdb/duckdb
      ref:
        description: "duckdb branch, tag, or full-length SHA"
        required: true
        type: string
        # oldest usable ref: 4ebeb16350eb2f819d682c581490af460dd0c995
        default: v1.5.1

jobs:
  compile-duckdb:
    name: Build DuckDB debug
    runs-on: ubuntu-24.04
    outputs:
        duckdb-sha: ${{ steps.get_duckdb_version.outputs.duckdb-sha }}

    steps:
      - name: print intpus
        run: |
          echo $GITHUB_SHA
          echo ${{ inputs.duckdbRepo }}
          echo ${{ inputs.ref }}

      - name: Checkout duckdb
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          repository: ${{ inputs.duckdbRepo }}
          ref: ${{ inputs.ref }}
          path: duckdb

      - name: Install DuckDB dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@main
        with:
          key: ${{ github.job }}

      - name: Build DuckDB
        working-directory: duckdb
        env:
          GEN: ninja
          EXPORT_DYNAMIC_SYMBOLS: 1
          BUILD_JEMALLOC: 1
        shell: bash
        run: |
          make debug

      - name: duckdb version
        id: get_duckdb_version
        working-directory: duckdb
        run: |
          ./build/debug/duckdb --version
          echo "duckdb-sha=$(git -P log -n 1 --pretty=format:%H)" >> "$GITHUB_OUTPUT"

      - name: rename duckdb binary
        run: |
          cp duckdb/build/debug/duckdb duckdb-debug

      - name: Store DuckDB binary
        uses: actions/upload-artifact@v6
        with:
          name: ${{ inputs.ref }}-duckdb-debug
          path: duckdb-debug

  compile-duckdb-afl:
    name: Compile DuckDB with AFL++ compiler
    runs-on: ubuntu-24.04
    env:
      CC: ${{ github.workspace }}/bin/afl-clang-fast
      CXX: ${{ github.workspace }}/bin/afl-clang-fast++
      DUCKDB_DIR: ${{ github.workspace }}/duckdb
      DUCKDB_AFLPLUSPLUS_DIR: ${{ github.workspace }}/duckdb_aflplusplus
    steps:
      - name: print intpus
        run: |
          echo $GITHUB_SHA
          echo ${{ inputs.duckdbRepo }}
          echo ${{ inputs.ref }}

      - name: Checkout duckdb_aflplusplus
        uses: actions/checkout@v6
        with:
          repository: duckdblabs/duckdb-aflplusplus
          path: duckdb_aflplusplus

      - name: Checkout aflplusplus
        id: checkout-aflplusplus
        uses: actions/checkout@v6
        with:
          repository: aflplusplus/aflplusplus
          ref: v4.30c
          path: aflplusplus

      - name: Checkout duckdb
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          repository: ${{ inputs.duckdbRepo }}
          ref: ${{ inputs.ref }}
          path: duckdb

      - uses: ./duckdb/.github/actions/cleanup_runner

      # https://github.com/AFLplusplus/AFLplusplus/blob/stable/docs/INSTALL.md
      - name: Install AFL++ dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3-dev automake cmake git flex bison libglib2.0-dev libpixman-1-dev python3-setuptools cargo libgtk-3-dev
          sudo apt-get install -y lld-14 llvm-14 llvm-14-dev clang-14 || sudo apt-get install -y lld llvm llvm-dev clang
          sudo apt-get install -y gcc-$(gcc --version|head -n1|sed 's/\..*//'|sed 's/.* //')-plugin-dev libstdc++-$(gcc --version|head -n1|sed 's/\..*//'|sed 's/.* //')-dev

      - name: Install DuckDB dependencies
        run: |
          sudo apt-get install -y ninja-build

      # /proc/sys/kernel/core_pattern needs to be adjusted, see:
      # https://github.com/AFLplusplus/AFLplusplus/blob/fb52b8edf86e12628df834f4bb144916b18a503d/src/afl-fuzz-init.c#L2476C1-L2489C70
      - name: Config step
        working-directory: ${{ env.DUCKDB_AFLPLUSPLUS_DIR }}
        run: |
          echo core | sudo tee /proc/sys/kernel/core_pattern

      # https://github.com/AFLplusplus/AFLplusplus/blob/stable/GNUmakefile
      - name: Cache AFL++ binaries
        id: cache-aflplusplus
        uses: actions/cache@v5
        with:
          path: |
            ${{ github.workspace }}/bin/afl*
            ${{ github.workspace }}/lib/afl/*
            ${{ github.workspace }}/share/doc/afl/*
            ${{ github.workspace }}/share/afl/*
            ${{ github.workspace }}/share/man/man8/*
            ${{ github.workspace }}/include/afl/*
            ${{ github.workspace }}/aflplusplus/afl-fuzz
            ${{ github.workspace }}/aflplusplus/afl-clang-fast
            ${{ github.workspace }}/aflplusplus/afl-clang-fast++
          key: ${{ runner.os }}-aflplusplus-${{ steps.checkout-aflplusplus.outputs.commit }}

      - name: build AFL++
        if: steps.cache-aflplusplus.outputs.cache-hit != 'true'
        working-directory: aflplusplus
        run: |
          sudo make PREFIX=${{ github.workspace }} source-only
          sudo make PREFIX=${{ github.workspace }} install

      - name: AFL++ version
        run: |
          ${{ github.workspace }}/bin/afl-fuzz --version
          $CC --version
          $CXX --version

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@main
        with:
          key: ${{ github.job }}

      - name: Compile DuckDB library with afl++ compiler
        run: |
          make -C $DUCKDB_AFLPLUSPLUS_DIR/src \
          DUCKDB_DIR=$DUCKDB_DIR \
          DUCKDB_AFLPLUSPLUS_DIR=$DUCKDB_AFLPLUSPLUS_DIR \
          CC=$CC \
          CXX=$CXX \
          BUILD_JEMALLOC=1 \
          USE_CCACHE=1 \
          duckdb-lib

      - name: duckdb version
        id: get_duckdb_version
        working-directory: ${{ env.DUCKDB_DIR }}
        run: |
          ./build/release/duckdb --version

      - name: rename duckdb binary
        run: |
            cp ${{ env.DUCKDB_DIR }}/build/release/duckdb duckdb-afl

      - name: Store DuckDB binary (compiled with AFL++)
        uses: actions/upload-artifact@v6
        with:
          name: ${{ inputs.ref }}-duckdb-afl
          path: duckdb-afl

  test-duckdb-binaries:
    name: Test DuckDB executable
    runs-on: ubuntu-24.04
    needs: [compile-duckdb-afl, compile-duckdb]
    steps:
      - name: download duckdb afl++ binary
        uses: actions/download-artifact@v6
        with:
          name: ${{ inputs.ref }}-duckdb-afl

      - name: test duckdb afl++ binary
        run: |
          ls -l
          chmod +x duckdb-afl && ./duckdb-afl --version

      - name: download debug duckdb binary
        uses: actions/download-artifact@v6
        with:
          name: ${{ inputs.ref }}-duckdb-debug

      - name: test duckdb debug binary
        run: |
          ls -l
          chmod +x duckdb-debug && ./duckdb-debug --version

      - run: echo "sha:" ${{ needs.compile-duckdb.outputs.duckdb-sha }}
